{
    "type": "object",
    "properties": {
        "string-input-6:1560": {
            "type": "string",
            "default": "",
            "description": "Absolute path to the python3 executable; do not change unless you want to use your own python environment outside the KNIME_docker_vnc container",
            "example": ""
        },
        "double-input-8:1556": {
            "type": "number",
            "default": 1.0,
            "description": "imputation factor used to multiply the calculated value (e.g. global median x imputation factor)",
            "example": 1.0
        },
        "double-input-11:1556": {
            "type": "number",
            "default": 1.0,
            "description": "imputation factor used to multiply the calculated value (e.g. global median x imputation factor)",
            "example": 1.0
        },
        "string-input-11:1560": {
            "type": "string",
            "default": "",
            "description": "Absolute path to the python3 executable; do not change unless you want to use your own python environment outside the KNIME_docker_vnc container",
            "example": ""
        },
        "boolean-input-1:1557": {
            "type": "boolean",
            "default": "false",
            "description": "whether to impute also rows having only values to be imputed in them (checkbox checked) or whether to ignore these during the imutation step (checkbox unchecked)",
            "example": false
        },
        "boolean-input-6:1559": {
            "type": "boolean",
            "default": "false",
            "description": "whether to remove not used columns or not; unchecked -> original dataframe will be returned with imputed values; checked -> dataframe of processed columns plus optional grouping column",
            "example": false
        },
        "boolean-input-11:1559": {
            "type": "boolean",
            "default": "false",
            "description": "whether to remove not used columns or not; unchecked -> original dataframe will be returned with imputed values; checked -> dataframe of processed columns plus optional grouping column",
            "example": false
        },
        "boolean-input-11:1557": {
            "type": "boolean",
            "default": "true",
            "description": "whether to impute also rows having only values to be imputed in them (checkbox checked) or whether to ignore these during the imutation step (checkbox unchecked)",
            "example": true
        },
        "double-input-5:1556": {
            "type": "number",
            "default": 1.0,
            "description": "imputation factor used to multiply the calculated value (e.g. global median x imputation factor)",
            "example": 1.0
        },
        "boolean-input-1:1559": {
            "type": "boolean",
            "default": "false",
            "description": "whether to remove not used columns or not; unchecked -> original dataframe will be returned with imputed values; checked -> dataframe of processed columns plus optional grouping column",
            "example": false
        },
        "boolean-input-6:1557": {
            "type": "boolean",
            "default": "false",
            "description": "whether to impute also rows having only values to be imputed in them (checkbox checked) or whether to ignore these during the imutation step (checkbox unchecked)",
            "example": false
        },
        "string-input-5:1560": {
            "type": "string",
            "default": "",
            "description": "Absolute path to the python3 executable; do not change unless you want to use your own python environment outside the KNIME_docker_vnc container",
            "example": ""
        },
        "boolean-input-5:1559": {
            "type": "boolean",
            "default": "false",
            "description": "whether to remove not used columns or not; unchecked -> original dataframe will be returned with imputed values; checked -> dataframe of processed columns plus optional grouping column",
            "example": false
        },
        "double-input-6:1556": {
            "type": "number",
            "default": 1.0,
            "description": "imputation factor used to multiply the calculated value (e.g. global median x imputation factor)",
            "example": 1.0
        },
        "boolean-input-5:1557": {
            "type": "boolean",
            "default": "true",
            "description": "whether to impute also rows having only values to be imputed in them (checkbox checked) or whether to ignore these during the imutation step (checkbox unchecked)",
            "example": true
        },
        "boolean-input-8:1559": {
            "type": "boolean",
            "default": "false",
            "description": "whether to remove not used columns or not; unchecked -> original dataframe will be returned with imputed values; checked -> dataframe of processed columns plus optional grouping column",
            "example": false
        },
        "string-input-8:1560": {
            "type": "string",
            "default": "",
            "description": "Absolute path to the python3 executable; do not change unless you want to use your own python environment outside the KNIME_docker_vnc container",
            "example": ""
        },
        "boolean-input-8:1557": {
            "type": "boolean",
            "default": "true",
            "description": "whether to impute also rows having only values to be imputed in them (checkbox checked) or whether to ignore these during the imutation step (checkbox unchecked)",
            "example": true
        },
        "double-input-1:1556": {
            "type": "number",
            "default": 1.0,
            "description": "imputation factor used to multiply the calculated value (e.g. global median x imputation factor)",
            "example": 1.0
        },
        "string-input-1:1560": {
            "type": "string",
            "default": "",
            "description": "Absolute path to the python3 executable; do not change unless you want to use your own python environment outside the KNIME_docker_vnc container",
            "example": ""
        }
    }
}